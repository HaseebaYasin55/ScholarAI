NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional auth/payment site URL override. When unset, the browser's own origin
# is used (localhost in dev, the current deployment domain in production). Do
# NOT point this at a deleted or old deployment.
NEXT_PUBLIC_SITE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
RESEND_API_KEY=
CRON_SECRET=